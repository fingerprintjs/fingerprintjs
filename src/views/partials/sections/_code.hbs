{{! prettier-ignore }}
<pre><code class='line-numbers language-javascript'>import FP from "fingerprintjs"; 

let fp = await FP.load({token});
let ip = await fp.get();

console.log(`VisitorID: ${id}`);</code></pre>